@*@model IEnumerable<FitPlaneLife.Domain.Entities.User.UAbonements>
@{
     ViewBag.Title = "Index";
}

<!DOCTYPE html>
<html>
<head>
     <title>Subscriptions</title>
</head>
<body>
     <h1>Subscription</h1>

     <table class="table table-striped">
          <thead>
               <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Date of birth</th>
               </tr>
          </thead>
          <tbody>
               @foreach (var abonement in Model)
               {
                    <tr>
                         <td>@abonement.Name</td>
                         <td>@abonement.Email</td>
                         <td>@abonement.Phone</td>
                         <td>@abonement.DateOfBirth</td>
                    </tr>
               }
          </tbody>
     </table>

     <div class="d-flex justify-content-center my-3">
          <a href="@Url.Action("Create", "Subscription")" class="btn btn-primary mx-2">Create Monthly Subscription</a>
          <a href="@Url.Action("Create", "Subscription")" class="btn btn-primary mx-2">Create Quarterly Subscription</a>
          <a href="@Url.Action("Create", "Subscription")" class="btn btn-primary mx-2">Create Half Yearly Subscription</a>
     </div>
</body>
</html>
*@